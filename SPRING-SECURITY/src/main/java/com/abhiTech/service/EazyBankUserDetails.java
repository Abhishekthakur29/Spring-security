package com.abhiTech.service;

//@Service
public class EazyBankUserDetails  {
//	implements UserDetailsService
	/*
	 * @Autowired BankRepository bankRepository;
	 * 
	 * @Override public UserDetails loadUserByUsername(String username) throws
	 * UsernameNotFoundException { String userName, password; List<GrantedAuthority>
	 * authorities; Customer customer = bankRepository.findByEmail(username);
	 * 
	 * if (customer == null) { throw new
	 * UsernameNotFoundException("User not found the with the given EmailId ::> " +
	 * username); } else { userName = customer.getEmail(); password =
	 * customer.getPwd(); authorities = new ArrayList<>(); authorities.add(new
	 * SimpleGrantedAuthority(customer.getRole())); } return new User(userName,
	 * password, authorities); }
	 */
}
